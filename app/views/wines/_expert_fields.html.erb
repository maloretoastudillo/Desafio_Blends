<div class="nested-fields d-flex align-items-start">
   <%= f.label "Evaluador:", class:'mr-2'%>
   <%= f.select :id, @experts.pluck(:id, :name) %>
   <%= link_to_remove_association "Eliminar", f %>
</div>
